{
  "//": "Merge these into webrcs-unleashed/manifest.json. Only the additions are listed; keep everything already there.",

  "permissions": ["storage", "offscreen"],

  "//permissions": "`offscreen` is what allows the hidden document that owns Web MIDI. There is no manifest key that pre-grants MIDI itself: access is requested at runtime from the extension's own (secure) origin, and SysEx must be granted once from the visible options page.",

  "options_page": "src/options/index.html",

  "background": {
    "service_worker": "src/background.js",
    "type": "module"
  },

  "//background": "If the extension has no service worker yet, this adds one whose only job is installMidiBridge(). If it already has one, import and call installMidiBridge() from it instead of adding this key.",

  "web_accessible_resources": [
    {
      "resources": ["src/*"],
      "matches": ["http://*/*", "https://*/*"]
    }
  ]
}
